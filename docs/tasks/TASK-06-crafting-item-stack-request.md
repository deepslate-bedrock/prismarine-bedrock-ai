# TASK 06 - Crafting Via Bedrock Item Stack Requests

- **Status:** `[/]` active
- **Owner:** Codex / 2026-05-13
- **Scope:** Track the Bedrock-native crafting path from planner output to `item_stack_request` execution.
- **Owned files:** `src/builtins/crafting.js`, `src/builtins/inventory.js`, `src/builtins/containers/index.js`, `scripts/decode-endstone-packet-recording.js`, `scripts/endstone-packet-recorder/src/endstone_packet_recorder/recorder.py`, `scripts/e2e-server/help.js`, `scripts/e2e-server/launch.js`, `scripts/e2e-server/options.js`, `scripts/e2e-server/process-utils.js`, `test/static/crafting.test.js`, `test/live/crafting.test.js`, `test/recorded-bds/scenarios/craft-wooden-pickaxes-at-table.json`, `docs/in-dev/crafting-util-implementation-notes.md`
- **Related docs:** `AGENTS.md`, `docs/in-dev/crafting-util-implementation-notes.md`, `test/rules.md`

## Goal

The bot should craft vanilla recipes through Bedrock `crafting_data` and `item_stack_request` while preserving server-authoritative inventory state and Bedrock stack identity.

## Non-Goals

- Do not reintroduce a local Bedrock planner fallback unless explicitly requested.
- Do not apply speculative inventory changes after crafting. Any local mirror update must be tied to an accepted server packet and its request context.
- Do not treat Geyser acceptance as proof that BDS accepts the same semantic request.

## Current Architecture

- `src/builtins/setup.js` subscribes to `crafting_data` and appends recipes to `botState.bedrockCraftingRecipes`.
- `src/builtins/crafting.js` uses `mineflayer-crafting-util` for planning and resolves each utility step against live Bedrock recipe data before execution.
- `buildActions()` emits either recipe-book auto craft actions or normal craft actions.
- `sendRequest()` sends Bedrock `item_stack_request` payloads and waits for matching `item_stack_response`.
- Static coverage in `test/static/crafting.test.js` asserts the action ordering for `craft_recipe_auto`, `craft_recipe`, `results_deprecated`, `consume`, and `take`.
- Live coverage in `test/live/crafting.test.js` exercises planning and crafting against the configured local server target.

## Current Plan

- `[x]` Verify the current uncommitted crafting implementation with focused static tests.
- `[x]` Compare normal craft packet shape against Endstone recorder output from a real 1.26.12 client.
- `[x]` Run focused live normal-crafting test against Endstone/BDS.
- `[ ]` Round-trip representative `craft_recipe_auto` and `craft_recipe` packets through `scripts/roundtrip-packet.js` if their standalone JSON examples are refreshed.
- `[ ]` Run broader live crafting coverage, including Java/Geyser, if the next task needs cross-target evidence.
- `[x]` Add Endstone packet-recorder player scoping so stray human joins do not contaminate bot packet evidence.

## Current State

- Normal inventory crafting now matches the real-client Endstone trace closely enough for BDS 1.26.12.2 to accept the requests.
- Full Endstone live crafting coverage still does not pass every test. Inventory planks crafting passes, but workbench pickaxe cases currently fail around bot-driven table setup/opening rather than the final pickaxe packet shape.
- A new human-driven Endstone recording scenario, `craft-wooden-pickaxes-at-table`, has been added under `test/recorded-bds/scenarios/` to capture the real client's workbench-open and wooden-pickaxe craft packets.
- The rerun of the human scenario includes `player_auth_input` packets. Compared with the bot's failed open attempt, the human client keeps `interact_rotation` and `camera_orientation` synchronized with the view direction before BDS emits the `workbench` `container_open`.
- The latest 1.26.12 human trace has zero client-to-server `move_player` packets around the workbench interaction. The lone nearby `move_player` is server-to-client teleport setup, so the crafting-table open path must not add a client `move_player` sync.
- The real-client scenario showed workbench open uses `player_action start_item_use_on` face `1`, `animate swing_arm` with `swing_source:"interact"`, then `inventory_transaction` `item_use` with eye-height `player_pos` (`y=65.62` when standing at `y=64`) and top-face `click_pos` approximately `{ x: 0.8456, y: 1, z: 0.2944 }`.
- The latest packet comparison showed the bot emitted an extra immediate `interact mouse_over_entity` before `player_action start_item_use_on`; the live test's final workbench-open sequence does not include that packet.
- The live test reports `player_auth_input.interaction_model` as `touch`; the bot temporarily reports `touch` too while narrowing packet differences.
- The live test's idle/open auth input keeps `block_breaking_delay_enabled` and `vertical_collision` set. The bot did not set the first flag anywhere, and it only copied `vertical_collision` from the last physics movement collision, which can be false while standing still on a supporting block.
- `mineflayer-crafting-util` plan handling uses the current `status` contract while keeping legacy `success` compatibility.
- Normal crafting opens the player inventory first, sends negative request IDs, uses `cause: -1`, omits `dynamic_container_id` in the observed craft slot refs, places inventory craft inputs in `crafting_input` slot `30` first, and uses the final request ID as the `creative_output` stack id.
- The accepted `item_stack_response` for `hotbar_and_inventory` does not include an item network ID. `crafting.js` now applies the accepted craft result using the request's `results_deprecated` item plus the server-confirmed count/stack id. `inventory.js` no longer clears non-empty response slots when the response lacks item type data.
- Endstone packet recorder now records `player_auth_input` like any other packet unless packet-id filtering excludes it. The decoder prints auth input as deltas by default so unchanged ticks do not flood analysis output.
- Endstone packet recorder can now be scoped with `E2E_PACKET_RECORDER_PLAYERS` / `--endstone-packet-recorder-player=NAME` so unrelated joined players do not write packet, join/quit, or scenario records. `E2E_PACKET_RECORDER_SPLIT_BY_PLAYER=1` / `--endstone-packet-recorder-split-by-player` also writes per-player JSONL files beside the combined recorder file.
- Non-loop immediate auth-input sends have been removed. Auth-input edits now ride the regular movement tick, and container-open look synchronization waits briefly for the main auth-input loop instead of forcing an immediate packet.
- The latest split-by-player observer capture shows BDS does not send `move_player` updates for OpBot's auth-input-only look changes to the human observer. OpBot's own client-to-server `player_auth_input` now reports synchronized `yaw`, `head_yaw`, `interact_rotation`, and `camera_orientation`; the visible-turn symptom is not caused by those fields being unset.
- Preserving the intentionally selected empty hotbar slot for workbench open changed the bot open transaction to `hotbar_slot: 1` with `held_item: air`, but BDS still did not emit `container_open`.
- The bot does receive human movement. In the split capture, OpBot receives `add_player` for `Generel7050` runtime `2` followed by repeated server-to-client `move_player` packets for runtime `2`. The missing broadcast is asymmetric: the human client does not receive movement/rotation updates for OpBot's auth-input-only look changes.
- A direct `MC_VERSION=1.26.12` run is currently blocked by missing local registry data (`Do not have data for bedrock_1.26.12`). The installed Bedrock data directories are `1.26.0`, `1.26.10`, and `1.26.20`.
- Latest direct `MC_VERSION=1.26.12` rerun for one wooden pickaxe still blocks before login with missing `bedrock_1.26.12` registry data.
- Latest runnable Endstone/BDS `1.26.12.2` rerun using installed `MC_VERSION=1.26.10` reaches the one-wooden-pickaxe test, but fails before any craft request because BDS never sends `container_open` for the crafting table.
- The same rerun confirms a separate auth-input falling symptom: before the setup teleport is acknowledged, decoded bot auth-input Y drops from about `1.62` to `-34.27`; after `/tp OpBot 0.0 64.0 0.0`, the bot reports `y=65.62` in the decoded deltas.
- A two-bot rotation check reproduced the visibility issue without a human client: `LookBot` sent auth-input yaw/head-yaw/camera-orientation changes, while `SentinelBot` only received `LookBot`'s `add_player` and setup teleport `move_player`; no server-to-client rotation/movement updates for `LookBot` were broadcast during the look sequence.
- The same two-bot check against Endstone/BDS `1.21.130.4` produced the same result: `LookBot` sent auth-input look changes, but `SentinelBot` did not receive a later `move_player`, `move_entity`, or `move_entity_delta` for `LookBot` after setup teleport.
- Rotation broadcast diagnosis has been split into `docs/tasks/TASK-12-bot-rotation-broadcast.md`; keep this task focused on crafting/workbench behavior.
- Setting bot auth input to `play_mode: screen` and `interaction_model: crosshair` did not fix workbench open and did not cause BDS to broadcast OpBot movement/rotation to the observer.
- Manual Endstone chat-command movement/look testing on BDS `1.26.12.2` found that `examples/basic-bot.js` receives chat and emits auth-input look changes, but its auth-input position drifts rapidly into invalid negative Y values. BDS does not broadcast `MyBot` movement/rotation updates back to the human observer, and human attack transactions against `MyBot` do not visibly knock it.

## Change Ledger

| File | State | Notes |
| --- | --- | --- |
| `src/builtins/crafting.js` | modified | Ported plan handling to `status`, added real-client normal-craft request shape, split normal crafts into one application per request, and applies accepted craft results from response/request context. |
| `src/builtins/inventory.js` | modified | Treats `hotbar_and_inventory` as a main inventory response container and avoids clearing non-empty slots when response data lacks item type. |
| `src/builtins/auth-input.js` | modified | `flushPlayerAuthInput()` no longer sends an immediate auth-input packet; queued edits are consumed by the next main movement tick. |
| `src/builtins/physics/index.js` | modified | Removed the immediate auth-input helper from the public bot state. |
| `src/builtins/containers/index.js` | modified | Aligns block-open `inventory_transaction` player position with real Bedrock client eye-height `player_pos`; waits briefly for the normal auth-input loop after look changes instead of forcing an immediate auth-input send. |
| `src/builtins/physics-constants.js` | modified | Added the Bedrock auth-input bit for `block_breaking_delay_enabled`. |
| `src/builtins/physics/input-controls.js` | modified | Sets `block_breaking_delay_enabled` during normal auth-input control evaluation. |
| `src/builtins/physics/index.js` | modified | Treats standing on a solid local support block as vertical collision/on-ground for auth-input flag purposes. |
| `src/builtins/physics/movement-packets.js` | modified | Sends outgoing `head_yaw` from the same interpolated yaw as `yaw` so auth-input look packets no longer jump head yaw ahead of body yaw. |
| `src/builtins/crafting.js` | modified | Preserves the empty hotbar slot selected before opening the crafting table so `openBlockContainer()` does not switch back to the first item stack. |
| `test/static/crafting.test.js` | modified | Updated normal-craft expectations for `place`, `hotbar_and_inventory`, slot `30`, and current/legacy plan status normalization. |
| `test/live/crafting.test.js` | modified | Focused oak-planks live tests on inventory crafting setup and `status === "complete"`; workbench setup now teleports with decimal coordinates so the bot click starts from the same `{x:0,z:0}` player position observed in the live test. |
| `test/static/food.test.js` | modified | Static food test now simulates the regular auth-input pre-send hook instead of `flushPlayerAuthInput()` sending a packet itself. |
| `docs/reference/mineflayer-feature-comparison.md` | modified | Removed the immediate auth-input helper from the documented exposed movement API. |
| `test/recorded-bds/scenarios/craft-wooden-pickaxes-at-table.json` | added | Human-driven workbench craft scenario for recording real-client crafting-table and wooden-pickaxe packet flow. |
| `scripts/decode-endstone-packet-recording.js` | modified | Streams input line-by-line, writes optional `--out` decoded JSONL, uses compact summary output by default, keeps `--packet-ids=` filtering, supports `--full`, and emits semantic `player_auth_input` deltas while ignoring tick/look/movement noise by default. |
| `scripts/endstone-packet-recorder/src/endstone_packet_recorder/recorder.py` | modified | Adds player allowlist filtering and optional per-player JSONL file writes. |
| `scripts/e2e-server/help.js`, `scripts/e2e-server/options.js`, `scripts/e2e-server/launch.js`, `scripts/e2e-server/process-utils.js` | modified | Adds launcher/env plumbing and help text for player-scoped and split-by-player Endstone packet recording. |
| `test/recorded-bds/README.md`, `docs/in-dev/e2e-server-launch-notes.md` | modified | Documents player-scoped and per-player Endstone packet recording commands. |

## Parallel Subtasks

| Subtask | Owner | Owned files | Expected output | Status |
| --- | --- | --- | --- | --- |
| Planner bridge review | Unassigned | `src/builtins/crafting.js`, `docs/in-dev/crafting-util-implementation-notes.md` | Confirm utility step resolution and live recipe matching constraints | `[ ]` |
| Packet shape verification | Unassigned | `scripts/roundtrip-packet.js`, temp JSON under `logs/` | Round-trip evidence for craft request shapes | `[ ]` |
| Live BDS evidence | Unassigned | `test/live/crafting.test.js`, `logs/` | Focused Endstone/BDS pass/fail with packet evidence if needed | `[ ]` |
| Live Geyser evidence | Unassigned | `test/live/crafting.test.js`, `ref/geyser/` notes only | Focused Java/Geyser pass/fail and translator notes if behavior diverges | `[ ]` |

## Evidence Log

- `2026-05-13` - Task log seeded from repository inspection. No tests run in this documentation-only pass.
- `2026-05-13` - Real-client Endstone recorder trace for normal oak-log to oak-planks craft showed:
  - `container_open` inventory window id `2`.
  - `item_stack_request -25`: `take` from `hotbar` slot `0` stack `5` to `cursor`.
  - `item_stack_request -27`: `place` from `cursor` to `crafting_input` slot `30`.
  - `item_stack_request -29`: `craft_recipe 272`, `results_deprecated`, `consume crafting_input slot 30 stack 5`, and `place creative_output slot 50 stack -29` to `hotbar_and_inventory` slot `0`; response `ok`.
- `2026-05-13` - `node -c src\builtins\crafting.js; node -c src\builtins\inventory.js; node -c scripts\decode-endstone-packet-recording.js` passed.
- `2026-05-13` - `python -m py_compile scripts\endstone-packet-recorder\src\endstone_packet_recorder\recorder.py` passed; generated tracked pyc was restored afterward.
- `2026-05-13` - `npx mocha test\static\crafting.test.js test\static\inventory-mirror.test.js` passed: 6 passing.
- `2026-05-13` - Focused Endstone live run passed:
  - Command: `$env:E2E_ENDSTONE_PACKAGE='endstone'; $env:MC_VERSION='1.26.10'; $env:E2E_PACKET_RECORD_FILE='logs/bot-normal-craft-endstone.jsonl'; node scripts/e2e-servers.js launch --target=endstone --world=superflat --endstone-packet-recorder --exit-after-client --client-timeout-ms=300000 --client node scripts/tmp/run-endstone-normal-craft.js`
  - Server: Endstone `0.11.3`, BDS `1.26.12.2`, bot protocol version `1.26.10`.
  - Result: focused live `normal crafts oak planks from oak logs with the live server recipe data` passed, 1 passing.
  - Observed bot requests: final craft requests used `crafting_input` slot `30`, `creative_output` stack ids `-29` and `-35`, and server responses were `ok`; local mirror reached `oak_planks x8`.
- `2026-05-13` - `pnpm run test:static` - PASS. Notes: full static suite passed, 47 passing.
- `2026-05-13` - Full Endstone live crafting run - FAIL. Command: `$env:E2E_ENDSTONE_PACKAGE='endstone'; $env:MC_VERSION='1.26.10'; $env:E2E_PACKET_RECORD_FILE='logs/bot-full-crafting-endstone.jsonl'; node scripts/e2e-servers.js launch --target=endstone --world=superflat --endstone-packet-recorder --exit-after-client --client-timeout-ms=600000 --client node node_modules/mocha/bin/mocha.js --config .mocharc.live.json test/live/crafting.test.js`. Server: Endstone `0.11.3`, BDS `1.26.12.2`. Result: 3 passing, 2 failing. Inventory oak-planks tests passed; recursive wooden-pickaxe tests failed around bot-driven workbench setup/opening.
- `2026-05-13` - Added human workbench scenario - PASS. Notes: `test/recorded-bds/scenarios/craft-wooden-pickaxes-at-table.json` parses successfully and will record a real client opening a crafting table, sending `item_stack_request`, and ending with three `minecraft:wooden_pickaxe` items.
- `2026-05-13` - Removed non-loop auth-input sends - PASS. Notes: repository search for the removed immediate auth-input helper found no matches after code/docs cleanup.
- `2026-05-13` - Syntax checks - PASS. Command: `node -c src\builtins\auth-input.js; node -c src\builtins\containers\index.js; node -c src\builtins\physics\index.js; node -c test\static\food.test.js`.
- `2026-05-13` - `npx mocha test\static\food.test.js` - PASS. Notes: 3 passing.
- `2026-05-13` - `pnpm run test:static` - PASS. Notes: full static suite passed, 48 passing.
- `2026-05-13` - Auth-input delta decode smoke - PASS. Notes: generated `scripts/tmp/player-auth-input-delta-sample.jsonl`; default decode produced 2 lines because one packet only changed ignored `tick`; final delta reported `yaw` and `head_yaw` changing from `0` to `90`.
- `2026-05-13` - Packet logging simplification checks - PASS. Notes: stale auth-input option search returned no matches; JS syntax checks for decode, relay, and e2e-server helper files passed; `python -m py_compile scripts\endstone-packet-recorder\src\endstone_packet_recorder\recorder.py` passed.
- `2026-05-13` - Compact decoder summary mode - PASS. Notes: decoder now streams input, omits full decoded params by default, produces packet-specific summaries for common inventory/container/action packets, keeps `--full --out` for targeted raw decoded params, and summarizes initial auth input without listing every false flag.
- `2026-05-13` - Auth-input decoded-data review - PASS. Notes: existing decoded files still showed many auth-input lines because look fields (`camera_orientation`, `interact_rotation`, `pitch`, `yaw`, `head_yaw`) changed every tick while the player aimed. Decoder default ignore list now treats tick/look/movement fields as noise and keeps semantic changes such as flags, play mode, and embedded requests visible.
- `2026-05-13` - Rerun human Endstone scenario - PASS. Raw log: `.e2e-servers/endstone-bds/logs/human-craft-wooden-pickaxes.jsonl` contains appended run with `player_auth_input` packet id `144` and `scenario_end status=complete`. Decoded focused traces: `logs/decoded-human-pickaxe-auth-open.jsonl` and `logs/decoded-bot-pickaxe-auth-open.jsonl`.
- `2026-05-13` - Workbench-open comparison - IN PROGRESS. Human trace before `container_open workbench` shows `interact_rotation` tracking yaw/pitch and `camera_orientation` tracking the view vector; latest bot trace changes `yaw`, `pitch`, and `head_yaw` only, leaving those auth-input fields at zero.
- `2026-05-13` - Focused Endstone pickaxe after auth orientation patch - FAIL. Command used `E2E_PACKET_RECORD_FILE=logs/bot-focused-pickaxe-orientation-auth.jsonl` and grep `crafts a wooden pickaxe through recursive multi-step planning`; result remained `Timed out waiting for container_open`. Decoded trace `logs/decoded-bot-pickaxe-orientation-open-packets.jsonl` confirmed `interact_rotation` and `camera_orientation` now update, but BDS still does not emit `container_open`.
- `2026-05-13` - Follow-up packet comparison - IN PROGRESS. Human top-face open uses a click position derived from the current view ray, e.g. local top-face coordinates around `x=0.61,z=0.45` in the auth-input scenario rerun. Bot top-face open sent fixed center `x=0.5,z=0.5` while its yaw/pitch ray from `player_pos=0.5,65.62,0.5` intersects closer to the west edge. Next edit derives block-open `click_pos` from the current view ray.
- `2026-05-13` - Focused Endstone pickaxe after ray-derived click and side-face selection - FAIL. Raw logs: `.e2e-servers/endstone-bds/logs/bot-focused-pickaxe-ray-click.jsonl` and `.e2e-servers/endstone-bds/logs/bot-focused-pickaxe-side-face.jsonl`. The side-face run sent `player_action start_item_use_on` face `4` and `inventory_transaction item_use` face `4`, but BDS still did not send `container_open`.
- `2026-05-13` - Focused Endstone pickaxe with human client connected - INVALID. Raw log: `.e2e-servers/endstone-bds/logs/bot-focused-pickaxe-human-standpos.jsonl`; `Generel7050` joined during the bot run, contaminating the packet stream and server state. Do not use this run as pass/fail evidence.
- `2026-05-13` - Look sync patch - PASS static checks. Added explicit `syncLook()` before block-container use; `node -c` passed for `src/builtins/physics/movement-packets.js`, `src/builtins/physics/index.js`, and `src/builtins/containers/index.js`; `npx mocha test/static/crafting.test.js test/static/food.test.js` passed with 9 passing.
- `2026-05-13` - Recorded scenario launcher version correction - PASS. Changed the default Endstone package from `endstone==0.10.18` to `endstone`, updated active recorded-scenario docs away from stale 1.21.130 decode examples, stopped the stale 1.21.130 BDS process, and relaunched `craft-wooden-pickaxes-at-table`. New run log `logs/scenario-craft-wooden-pickaxes-20260513-182357.out.log` confirms BDS `1.26.12.2`, Endstone `0.11.3`, packet recorder active at `.e2e-servers/endstone-bds/logs/packet-recorder.jsonl`.
- `2026-05-13` - Workbench open packet review - PASS. Decoded human trace `logs/decoded-human-pickaxe-12612-all-full.jsonl` shows no client-to-server `move_player`; successful open packet sequence is `player_action start_item_use_on` face `1`, `animate` interact, `inventory_transaction item_use` face `1` with top-face `click_pos` `{x:0.8455779552459717,y:1,z:0.2944147288799286}`, then server `container_open workbench`. Bot setup should mirror this and avoid adding client `move_player`.
- `2026-05-13` - Focused static checks after top-face workbench-open patch - PASS. Command: `npx mocha test\static\crafting.test.js test\static\food.test.js`; result: 9 passing.
- `2026-05-13` - Workbench setup position patch - PASS static checks. Updated `test/live/crafting.test.js` to use decimal `/tp` coordinates for workbench setup so the bot is placed at the live-test click position instead of BDS-centering integer coordinates at `{x:0.5,z:0.5}`. Command: `node -c test\live\crafting.test.js; npx mocha test\static\crafting.test.js`; result: syntax pass and 6 passing.
- `2026-05-13` - Focused Endstone pickaxe after decimal stand position - FAIL. Command used `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-decimal-standpos.jsonl` and grep `crafts a wooden pickaxe through recursive multi-step planning`; server log confirms `/tp OpBot 0.0 64.0 0.0` placed the bot at `0.00, 64.00, 0.00`. Decoded trace `logs/decoded-bot-pickaxe-decimal-standpos-full.jsonl` shows the open transaction now has `player_pos {x:0,y:65.62000274658203,z:0}` and the recorded top-face `click_pos`, but BDS still did not send `container_open`.
- `2026-05-13` - Removed extra block-open mouse-over interact - VERIFIED in focused bot run. Updated `src/builtins/containers/index.js` so block-container opens begin with `player_action start_item_use_on`, matching the live test's final workbench-open packet sequence.
- `2026-05-13` - Temporary interaction-model alignment - VERIFIED in focused bot run. Updated `src/builtins/physics/movement-packets.js` to send `player_auth_input.interaction_model = touch` (`0`) instead of `crosshair` (`1`) to match the live test trace while debugging workbench open.
- `2026-05-13` - Focused Endstone pickaxe after removing mouse-over and using touch interaction model - FAIL. Command used `--endstone-packet-recorder-player=OpBot` with `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-no-mouseover-touch.jsonl`, so the human client `Generel7050` joined without contaminating the packet file. Server: Endstone `0.11.3`, BDS `1.26.12.2`. Decoded trace `logs/decoded-bot-pickaxe-no-mouseover-touch-full.jsonl` confirms no `interact mouse_over_entity`, `interaction_model:"touch"`, then `player_action start_item_use_on`, `animate interact`, and `inventory_transaction item_use`; BDS still did not send `container_open`.
- `2026-05-13` - Auth-input grounded flag fix - PASS static checks. Added `block_breaking_delay_enabled` to the auth-input control flags and made the physics tick preserve vertical collision/on-ground when the player is standing on a solid local support block. Command: `node -c src\builtins\physics-constants.js; node -c src\builtins\physics\input-controls.js; node -c src\builtins\physics\index.js; npx mocha test\static\crafting.test.js test\static\food.test.js`; result: syntax pass and 9 passing.
- `2026-05-13` - Auth-input packet-level baseline flags - PASS static checks. The control flag path worked in isolation but the focused bot run still encoded `input_data: 0`, so `src/builtins/physics/movement-packets.js` now ORs `block_breaking_delay_enabled` and `vertical_collision` directly into outgoing `player_auth_input` when the bot is grounded or standing on a solid support block. Command: `node -c src\builtins\physics\movement-packets.js; npx mocha test\static\crafting.test.js test\static\food.test.js`; result: syntax pass and 9 passing.
- `2026-05-13` - Auth-input object flag encoding - PASS static checks. A follow-up bot run still decoded `input_data: 0`, so `src/builtins/physics/movement-packets.js` now emits `input_data` as an explicit bitflag object, matching the hook style used elsewhere, instead of a raw bigint. Command: `node -c src\builtins\physics\movement-packets.js; npx mocha test\static\crafting.test.js test\static\food.test.js`; result: syntax pass and 9 passing.
- `2026-05-13` - Focused Endstone pickaxe after auth-input object flags - FAIL. Command used `--endstone-packet-recorder-player=OpBot` with `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-auth-flags-object.jsonl`. Decoded trace `logs/decoded-bot-pickaxe-auth-flags-object-full.jsonl` verifies `input_data` now reaches the live-test value `1407374883553280` (`block_breaking_delay_enabled=true`, `vertical_collision=true`) before open, but BDS still did not send `container_open`.
- `2026-05-13` - Latest focused Endstone pickaxe bot rerun - FAIL. Command used `--endstone-packet-recorder-player=OpBot` with `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-latest.jsonl`. Decoded trace `logs/decoded-bot-pickaxe-latest-full.jsonl` again shows `input_data` reaches `1407374883553280`, no `interact mouse_over_entity`, `player_action start_item_use_on`, `animate interact`, and `inventory_transaction item_use`, but no `container_open`.
- `2026-05-13` - Focused Endstone pickaxe retest - FAIL. Command used `--endstone-packet-recorder-player=OpBot` with `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-retest.jsonl`; `Generel7050` joined during the run but the recorder remained filtered to `OpBot`. Decoded trace `logs/decoded-bot-pickaxe-retest-full.jsonl` shows the same current packet shape and auth flags, but BDS still did not send `container_open`.
- `2026-05-13` - Teleport auth-input ack fix - PASS static checks. Live test sends `player_auth_input.handled_teleport=true` for one tick after scenario teleport; bot did not. Added `BIT_HANDLED_TELEPORT`, set a one-shot `_handledTeleportPending` flag on server `move_player` teleport, and emit `handled_teleport` once in outgoing auth input. Command: `node -c src\builtins\physics-constants.js; node -c src\builtins\physics\movement-packets.js; node -c src\builtins\physics\index.js; npx mocha test\static\crafting.test.js test\static\food.test.js`; result: syntax pass and 9 passing.
- `2026-05-13` - Packet-recorder scoping - PASS static checks. Added capture-time player filtering and optional per-player JSONL output. Commands: `node -c scripts/e2e-server/options.js; node -c scripts/e2e-server/launch.js; node -c scripts/e2e-server/process-utils.js; node -c scripts/e2e-server/help.js; node -c scripts/decode-endstone-packet-recording.js`; `python -m py_compile scripts/endstone-packet-recorder/src/endstone_packet_recorder/recorder.py`; option parse smoke with `--endstone-packet-recorder-player=OpBot,Generel7050 --endstone-packet-recorder-player Alt --endstone-packet-recorder-split-by-player`. Result: syntax/compile passed and parsed players as `OpBot,Generel7050,Alt` with split enabled.
- `2026-05-13` - Head-yaw sync and observer split capture - FAIL for crafting open, PASS for packet evidence. Commands: `node -c src\builtins\physics\movement-packets.js`; `npx mocha test\static\crafting.test.js test\static\food.test.js`; focused Endstone run with `--endstone-packet-recorder-split-by-player` and `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-headyaw-sync-split.jsonl`. Static checks passed, 9 passing. Focused live test still timed out waiting for `container_open`. Decoded files: `logs/decoded-bot-pickaxe-headyaw-sync-opbot-auth-look.jsonl`, `logs/decoded-bot-pickaxe-headyaw-sync-observer-players.jsonl`. OpBot auth input now keeps `head_yaw` aligned with `yaw` during look interpolation. Observer file shows `add_player` for OpBot at yaw/head_yaw `0` and no server `move_player` updates for OpBot runtime `1` during the bot's auth-input look.
- `2026-05-13` - Empty-hand workbench-open attempt - FAIL. Commands: `node -c src\builtins\crafting.js src\builtins\physics\movement-packets.js`; `npx mocha test\static\crafting.test.js test\static\food.test.js`; focused Endstone run with `--endstone-packet-recorder-player=OpBot` and `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-empty-hand-open.jsonl`. Static checks passed, 9 passing. Focused live test still timed out waiting for `container_open`. Decoded file `logs/decoded-bot-pickaxe-empty-hand-open-full.jsonl` confirms `player_action start_item_use_on`, `animate interact`, and `inventory_transaction item_use` with `hotbar_slot: 1`, `held_item` air, eye-height `player_pos`, recorded top-face `click_pos`, and no `container_open`.
- `2026-05-13` - Bot receiving human movement check - PASS. Decoded `logs/decoded-bot-pickaxe-headyaw-sync-opbot-movement-all.jsonl` from `.e2e-servers/endstone-bds/logs/bot-focused-wooden-pickaxe-headyaw-sync-split.OpBot.jsonl`; OpBot receives `add_player` for `Generel7050` runtime `2` and many server-to-client `move_player` packets for runtime `2`.
- `2026-05-13` - Attempted `MC_VERSION=1.26.12` focused Endstone run - BLOCKED before login. Command used `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-12612-screen-crosshair.jsonl` and `MC_VERSION=1.26.12`; result: Mocha before hook failed with `Do not have data for bedrock_1.26.12` from `prismarine-registry`.
- `2026-05-13` - Focused Endstone pickaxe with bot `play_mode: screen` and `interaction_model: crosshair` on `MC_VERSION=1.26.10` - FAIL. Command used split-by-player recorder with `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-screen-crosshair.jsonl`; result remained `Timed out waiting for container_open`. Decoded `logs/decoded-bot-pickaxe-screen-crosshair-opbot-full.jsonl` confirms outgoing `player_auth_input` reports `play_mode:"screen"` and `interaction_model:"crosshair"` and the open transaction uses `hotbar_slot: 1`, `held_item` air. Decoded `logs/decoded-bot-pickaxe-screen-crosshair-observer-movement.jsonl` contains `add_player` for OpBot but no `move_player`, `move_entity`, or `move_entity_delta` for OpBot runtime `1`.
- `2026-05-13` - Manual chat-command movement/look session started - IN PROGRESS. Background launcher process started Endstone `0.11.3`, BDS `1.26.12.2`, superflat world, packet recorder enabled with split-by-player output, and client `node examples/basic-bot.js`. Server log: `.e2e-servers/runs/2026-05-13T23-15-21-334Z/server-session.jsonl`; packet files: `.e2e-servers/endstone-bds/logs/chat-command-bot-12612-20260513-191518-packets.jsonl`, `.MyBot.jsonl`, and `.Generel7050.jsonl`; wrapper logs: `logs/chat-command-bot-12612-20260513-191518.out.log` and `.err.log`. Evidence seen so far: server ready on UDP `19132`, `MyBot joined the game`, `Generel7050 joined the game`, server received `<Generel7050> !lookAtMe`, and the bot logged `Looked at Generel7050`.
- `2026-05-13` - Manual chat-command movement/look packet review - FAIL for visible movement/look. Decoded files: `logs/decoded-chat-command-mybot-full.jsonl`, `logs/decoded-chat-command-human-full.jsonl`, `logs/decoded-chat-command-mybot-auth-look-full.jsonl`, and `logs/decoded-chat-command-human-movement-look-full.jsonl`. Findings: `!lookAtMe` reached the bot at `23:15:38`, and `MyBot` sent a `player_auth_input` look delta with `yaw ~= 133.61`, `head_yaw ~= 133.61`, `pitch ~= -89.92`, `interact_rotation` matching, and `camera_orientation` near straight up. The target pitch is bad because `MyBot`'s auth-input position had already fallen from valid spawn height to about `y=-682.58`; across the session decoded auth-input position Y ranged from about `1.54` to `-11523.18`. The human client sent 14 `inventory_transaction item_use_on_entity` attacks against `entity_runtime_id: "1"` (`MyBot`) from `23:15:42` through `23:16:03`, but decoded human observer packets contained zero server-to-client `move_player`, `move_entity`, or `move_entity_delta` packets for runtime `1`. This reproduces the user observation: the bot did not visibly rotate or move when hit.
- `2026-05-13` - Manual Endstone session cleanup - PASS. Command: `node scripts/e2e-servers.js cleanup-orphans --include-managed`; stopped the background launcher for the chat-command run.
- `2026-05-13` - Direct one-pickaxe `MC_VERSION=1.26.12` rerun - BLOCKED before login. Command used `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-12612-rerun.jsonl`, `MC_VERSION=1.26.12`, and grep `crafts a wooden pickaxe through recursive multi-step planning`; server started as Endstone `0.11.3`, BDS `1.26.12.2`, but Mocha before hook failed with `Do not have data for bedrock_1.26.12` from `prismarine-registry`.
- `2026-05-13` - Runnable one-pickaxe rerun against Endstone/BDS `1.26.12.2` using installed `MC_VERSION=1.26.10` - FAIL before crafting. Command used `E2E_PACKET_RECORD_FILE=logs/bot-focused-wooden-pickaxe-12612-server-rerun.jsonl`, `MC_VERSION=1.26.10`, and grep `crafts a wooden pickaxe through recursive multi-step planning`; result: `Timed out waiting for container_open`. Raw packet file: `.e2e-servers/endstone-bds/logs/bot-focused-wooden-pickaxe-12612-server-rerun.jsonl`. Decoded files: `logs/decoded-bot-focused-wooden-pickaxe-12612-server-rerun-full.jsonl` and `logs/decoded-bot-focused-wooden-pickaxe-12612-auth-position.jsonl`. The decoded auth-input trace shows a separate falling symptom before teleport ack (`position.y` falls from about `1.62` to `-34.27`), then the setup teleport snaps auth-input back to `y=65.62`; no `item_stack_request` craft packet is reached because the workbench does not open.
- `2026-05-13` - Two-bot rotation sentinel check - PASS for evidence, FAIL for visible rotation broadcast. Temporary runner: `scripts/tmp/two-bot-rotation-check.js` (gitignored). Command used `E2E_PACKET_RECORD_FILE=logs/two-bot-rotation-check-12612.jsonl`, `MC_VERSION=1.26.10`, `--endstone-packet-recorder-split-by-player`, and client `node scripts/tmp/two-bot-rotation-check.js`; server: Endstone `0.11.3`, BDS `1.26.12.2`; client exited `0`. Raw split packet files: `.e2e-servers/endstone-bds/logs/two-bot-rotation-check-12612.LookBot.jsonl` and `.SentinelBot.jsonl`. Decoded files: `logs/decoded-two-bot-rotation-lookbot-auth.jsonl`, `logs/decoded-two-bot-rotation-sentinel-all.jsonl`, and `logs/decoded-two-bot-rotation-sentinel-movement.jsonl`. `LookBot` sent many `player_auth_input` deltas with yaw/head-yaw/camera-orientation changes, including yaw stepping through `0 -> 90 -> 180 -> -90 -> 45 -> -135 -> 0`. `SentinelBot` received `add_player` for `LookBot` runtime `1` with yaw/head-yaw/pitch `0`, plus the setup teleport `move_player` for runtime `1`, but no later `move_player`, `move_entity`, or `move_entity_delta` packets for `LookBot` during the look sequence.
- `2026-05-13` - Two-bot rotation sentinel check on Endstone/BDS `1.21.130.4` - PASS for evidence, FAIL for visible rotation broadcast. Command used `E2E_ENDSTONE_PACKAGE=endstone==0.10.18`, `MC_VERSION=1.21.130`, `E2E_PACKET_RECORD_FILE=logs/two-bot-rotation-check-121130.jsonl`, `--endstone-packet-recorder-split-by-player`, and client `node scripts/tmp/two-bot-rotation-check.js`; server: Endstone `0.10.18`, BDS `1.21.130.4`; client exited `0`. Raw split packet files: `.e2e-servers/endstone-bds/logs/two-bot-rotation-check-121130.LookBot.jsonl` and `.SentinelBot.jsonl`. Decoded files: `logs/decoded-two-bot-rotation-121130-lookbot-auth.jsonl`, `logs/decoded-two-bot-rotation-121130-sentinel-all.jsonl`, and `logs/decoded-two-bot-rotation-121130-sentinel-movement.jsonl`. `LookBot` sent auth-input yaw/head-yaw/camera-orientation deltas through the same look sequence. `SentinelBot` received `add_player` for `LookBot` runtime `1` with yaw/head-yaw/pitch `0`, then the setup teleport `move_player` for runtime `1`, but no later `move_player`, `move_entity`, or `move_entity_delta` for runtime `1` during the look sequence. The Sentinel trace did contain `move_entity_delta` packets for passive mobs, confirming the recorder/decoder captured entity movement updates when BDS sent them.

## Protocol Notes

- Verify packet shape in `node_modules/minecraft-data/minecraft-data/data/bedrock/1.26.10/proto.yml` and `types.yml` before changing craft packet construction. This is the installed protocol data used against BDS 1.26.12.
- Use `ref/gophertunnel/minecraft/protocol/item_stack.go` for craft action enum and response status context.
- Use `ref/gophertunnel/minecraft/protocol/packet/crafting_data.go` and `ref/gophertunnel/minecraft/protocol/recipe.go` when validating recipe packet semantics.
- Use string enum names such as `craft_recipe`, `craft_recipe_auto`, `consume`, and `take`.
- Include `custom_names: []` and `cause: 'chat_public'` in `ItemStackRequest`.
- Include `dynamic_container_id` in `FullContainerName` slot objects unless a packet round-trip and live server test prove a narrower shape is accepted.

## Known Risks

- `crafting_data` recipe IDs and live item runtime IDs can differ from static planning IDs. Keep planning IDs and wire/runtime IDs conceptually separate.
- Geyser may accept or route a request differently from standalone BDS. Treat divergence as a system-under-test finding, not immediate proof that local packet shape is wrong.
- Server inventory update timing can make multi-step crafting fail if the next step starts before authoritative inventory packets settle.

## Handoff

First, inspect the current dirty changes in `src/builtins/crafting.js`, `test/static/crafting.test.js`, and `test/live/crafting.test.js` before editing. Then run static crafting tests and record exact results here. If packet shape changes, add a temporary JSON packet under `logs/`, run `node scripts/roundtrip-packet.js .\logs\<file>.json`, and summarize the parsed actions without committing the raw log.

## Resume Notes

- Next step, if continuing this task: run the broader live crafting suite or Java/Geyser target to check for regressions outside Endstone normal inventory crafting.
- Raw logs: `.e2e-servers/endstone-bds/logs/bot-normal-craft-endstone.jsonl` contains appended bot recorder runs; decode focused packets with `node scripts/decode-endstone-packet-recording.js .e2e-servers\endstone-bds\logs\bot-normal-craft-endstone.jsonl 1.26.10 --packet-ids=46,147,148`.
- Do not repeat: the real-client packet shape mismatch for slot `30` and `creative_output.stack_id = request_id` has already been validated against Endstone.

## Final Summary

Normal inventory crafting of oak planks from oak logs now succeeds against Endstone/BDS 1.26.12.2 using the current 1.26.10 protocol data. Broader crafting coverage remains a follow-up.

## Failure Summary

Not blocked.

## Completion Checklist

- `[x]` Task log updated with final evidence.
- `[x]` Current State and Change Ledger reflect the worktree.
- `[x]` Resume Notes say exactly what to do next, or Final Summary is filled.
- `[x]` `pnpm run test:static` or focused equivalent passed.
- `[ ]` Packet round-trip run for changed craft packet shapes.
- `[x]` Focused live crafting test run against at least one target, or deferred with reason.
- `[x]` Raw debug logs kept out of git.
